#deploy to framworkserver
name: Build and deploy to qpon prod server

on:
  workflow_dispatch:
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11

    - name: Build with Maven
      #run: mvn -B clean install --file pom.xml
      run: mvn clean compile package #-DskipTests

  notify:
    if: "${{ failure() }}"
    runs-on: ubuntu-latest
    needs: build
    steps:
      
        - uses: someimportantcompany/github-actions-slack-message@v1
          id: slack
          with:
            channel: C02G2E2PWBZ
            bot-token: xoxe.xoxp-1-Mi0yLTI1NDY0Nzg4MDIxOTktMjU3Mzc4MDIxNTc2MS0yOTA2Njg5ODU1OTQwLTI4OTc3MTcyNzExNTgtZjg5ZTY4ZWZkODQ1ZDlhMTcxNDRkNDBhYWViYTA5NmUwY2E4OGJiMTMwMmUyODFlMTkxNThjODlmNGRjOTAzNQ
            text: Deployment started
